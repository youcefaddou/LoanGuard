model Payment {
  id        Int      @id @default(autoincrement())
  loanId    Int
  amount    Float
  date      DateTime
  status    String   @default("ON_TIME") // ON_TIME, LATE, PARTIAL
  loan      Loan     @relation(fields: [loanId], references: [id])
}